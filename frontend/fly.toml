app = "securethecloud-ai-governance-board"
primary_region = "iad"

[build]
  [build.args]
    NEXT_PUBLIC_API_BASE = "https://securethecloud-ai-governance-board-api.fly.dev"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
