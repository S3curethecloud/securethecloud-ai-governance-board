services:
  backend:
    build: ./backend
    ports:
      - "8010:8000"
    environment:
      - LAB_MODE=true

  frontend:
    build: ./frontend
    ports:
      - "3010:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8010
    depends_on:
      - backend
